# MiniFlu Sync Panel

IPTV stream sync management panel built with Vite, React, TypeScript, shadcn-ui, and Tailwind CSS.

## Setup

```bash
npm install
npm run build
```

## Development

```bash
npm run dev
```

Runs on port 8080 by default.
